@model IEnumerable

@using(Html.BeginForm("ExportWithDataCellBandsResult", "Exporting")) {
    @Html.Partial("ExportWithDataCellBandsOptionsPartial", Model)
    @Html.Partial("ExportWithDataCellBandsPartial", Model)
    <input type="hidden" class="format" style="display: none;" />
}
@section AdditionalResources {
    <script>
        function submitForm(s, e) {
            var input = $('input.format');
            var form = input.closest('form');
            input.attr('name', s.name);
            form.submit();
        }
    </script>
}