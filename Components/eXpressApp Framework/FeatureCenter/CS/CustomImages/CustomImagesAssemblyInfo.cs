using System;
using System.Collections.Generic;
using System.Text;

namespace CustomImages {
	public static class CustomImagesAssemblyInfo {
		public static string AssemblyName {
			get { return typeof(CustomImagesAssemblyInfo).Assembly.GetName().Name; }
		}
	}
}
