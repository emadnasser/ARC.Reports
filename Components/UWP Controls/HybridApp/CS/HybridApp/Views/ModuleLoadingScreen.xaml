<Page
    x:Class="HybridApp.Views.ModuleLoadingScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    mc:Ignorable="d">
    <Grid Background="#FFFFFFFF" VerticalAlignment="Stretch" Opacity="0.5">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <ProgressRing Grid.ColumnSpan="2" Width="100" Height="100" IsActive="True" Visibility="{Binding ElementName=root, Path=Visibility}"/>
            <TextBlock Text="Loading..." Foreground="{StaticResource grayForegroundBrush}" HorizontalAlignment="Center" FontSize="22"/>
        </StackPanel>
    </Grid>
</Page>