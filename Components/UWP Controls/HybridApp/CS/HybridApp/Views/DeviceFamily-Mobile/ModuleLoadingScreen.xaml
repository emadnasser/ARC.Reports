<Page
    x:Class="HybridApp.Views.ModuleLoadingScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HybridApp.Views.DeviceFamily_Mobile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" HorizontalAlignment="Stretch">
            <ProgressBar Grid.ColumnSpan="2" IsIndeterminate="True" Visibility="{Binding ElementName=root, Path=Visibility}"/>
            <TextBlock Text="Loading..." Foreground="{StaticResource GrayForegroundBrush}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>